<%= render partial: 'form', locals: { workshop: @workshop, reservation: @reservation, url: "/admin/reservations/#{@reservation.id}"} %>

<%= link_to "Delete Workshop", "/admin/workshops/#{(workshop_id: @workshop.id)}" %>
